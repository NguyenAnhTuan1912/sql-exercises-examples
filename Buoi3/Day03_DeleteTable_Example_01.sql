USE EMPLOYEES;

-- Delete table or records in table.
/*
  Delete all records in Table, but the table.
  TRUNCATE TABLE [TableName];

  Delete all records in Table and itself (or Delete table).
  DROP TABLE [TableName];
*/

TRUNCATE TABLE [Employees];
DROP TABLE [Employees];